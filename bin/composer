#!/bin/bash

NAME='local_composer'

docker container start $NAME &>/dev/null || docker container run \
    --user 1001 \
    -d -i -v /home/$USER:/home/$USER \
    --name $NAME \
    local_composer tail -f /dev/null &>/dev/null

docker container exec -i -u $USER -w $(pwd) $NAME composer "$@"

